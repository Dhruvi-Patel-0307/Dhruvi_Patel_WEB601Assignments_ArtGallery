import { TypefilteroutPipe } from './typefilterout.pipe';

describe('TypefilteroutPipe', () => {
  it('create an instance', () => {
    const pipe = new TypefilteroutPipe();
    expect(pipe).toBeTruthy();
  });
});
